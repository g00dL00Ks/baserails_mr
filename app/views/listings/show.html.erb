

<%= image_tag @listing.image.url(:medium) %>
<p>
  <strong>Name:</strong>
  <%= @listing.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @listing.description %>
</p>

<p>
  <strong>Price:</strong>
  <%= @listing.price %>
</p>
<%= link_to "Buy Now", new_listing_order_path(@listing), class: "btn btn-primary" %><br/>
<br/>

<% if current_user == @listing.user %>
	<%= link_to 'Edit', edit_listing_path(@listing) %> |
<% end %>
<%= link_to 'Back', listings_path %>
